import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test — belum ada Firebase mock', (tester) async {
    // App memerlukan Firebase yang sudah diinisialisasi.
    // Tes integrasi penuh ada di integration_test/.
    expect(true, isTrue);
  });
}
